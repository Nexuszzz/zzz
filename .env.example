# Database Configuration (Neon PostgreSQL)
# DATABASE_URL = Pooled connection (for app queries)
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.aws.neon.tech/apm_portal?sslmode=require

# DIRECT_URL = Direct connection (for Prisma migrations)
DIRECT_URL=postgresql://user:password@ep-xxx.aws.neon.tech/apm_portal?sslmode=require

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="APM Portal"

# Revalidation Secret (for webhook)
REVALIDATION_SECRET=your-secret-key-here

# Database Direct Connection (for registrasi API)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=apm_portal
DB_USER=apm_user
DB_PASSWORD=apm_secure_password_2026
